<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emobi.diceapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".dicegame.DiceActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".openglcode.OpenGLDemoActivity"></activity> -->
        <!-- <activity android:name=".pack1.RotationActivity"></activity> -->
        <!-- <activity android:name=".pack3.MyGLActivity"></activity> -->
        <activity
            android:name=".pack3.ResultImage"
            android:screenOrientation="portrait" />
        <!-- <activity android:name=".dicegame.DiceActivity"></activity> -->
        <!-- <activity android:name=".twodice.TwoDiceActivity"></activity> -->
        <activity android:name=".PaypalIntegration"></activity>
    </application>

</manifest>